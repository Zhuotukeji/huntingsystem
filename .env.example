# Optional. Without a key the system uses its deterministic demo agent.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6

# Local persistent database.
DATABASE_PATH=.data/hunting.db

# Demo mode seeds synthetic companies, people, evidence, and tasks.
DEMO_MODE=true
